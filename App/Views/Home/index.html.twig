{% extends "base.html.twig" %}
{% block title %}Home{% endblock %}
{% block body %}

    <h1>Welcome</h1>

    {% if current_user %}
        Hello {{ current_user.username }}.
         <a href="logout">Log out</a>
    {% else %}
        <a href="login">Login</a> or <a href="accounts/register">Register</a>
    {% endif %}

{% endblock %}